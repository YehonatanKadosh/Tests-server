const questionTypes = {
  SingleChoice: "one answer only",
  MultipleSelection: "one or more answers",
};

const orientationTypes = {
  Horizontal: "horizontal",
  Vertical: "vertical",
};

const languages = {
  Hebrew: "hebrew",
  English: "english",
};

const roles = {
  Admin: "admin",
  User: "user",
};

module.exports = { questionTypes, orientationTypes, languages, roles };
